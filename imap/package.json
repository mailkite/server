{
  "name": "@mailkite/imap-edge",
  "version": "0.1.0",
  "private": true,
  "description": "MailKite IMAP edge - an imap-core daemon that serves stored mail over IMAPS (993), backed by the /api/imap/* backend contract.",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "imap-core": "^1.0.0",
    "redis": "^4.6.0"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "AGPL-3.0-only"
}
