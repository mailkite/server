{
  "name": "@mailkite/mta-submit",
  "version": "0.1.0",
  "private": true,
  "description": "MailKite SMTP submission edge - Haraka :587/:465 AUTH relay that POSTs authenticated outbound mail to the backend relay endpoint.",
  "scripts": {
    "start": "haraka -c .",
    "dev": "haraka -c .",
    "lint": "eslint .",
    "test": "node --test \"test/*.test.mjs\""
  },
  "dependencies": {
    "Haraka": "^3.3.1"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.6.0",
    "globals": "^17.7.0"
  },
  "license": "AGPL-3.0-only"
}
